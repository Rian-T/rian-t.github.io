---
import "../styles/global.css";

interface Props {
  title: string;
  bodyClass?: string;
  description?: string;
  ogImage?: string;
  ogImageAlt?: string;
  ogImageCard?: "summary" | "summary_large_image";
  ogType?: "website" | "article";
  publishedTime?: string;
  modifiedTime?: string;
}

const {
  title,
  bodyClass,
  description,
  ogImage,
  ogImageAlt,
  ogImageCard = "summary_large_image",
  ogType = "website",
  publishedTime,
  modifiedTime,
} = Astro.props;
const site = "https://rian-t.github.io";
const ogImageUrl = ogImage ? site + ogImage : undefined;
const canonical = site + Astro.url.pathname;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <title>{title}</title>
    <link rel="canonical" href={canonical} />
    <meta property="og:url" content={canonical} />
    <meta property="og:title" content={title} />
    <meta property="og:type" content={ogType} />
    <meta property="og:site_name" content="Rian Touchent" />
    <meta property="og:locale" content="en_US" />
    {description && <meta name="description" content={description} />}
    {description && <meta property="og:description" content={description} />}
    {ogType === "article" && publishedTime && (
      <meta property="article:published_time" content={publishedTime} />
    )}
    {ogType === "article" && modifiedTime && (
      <meta property="article:modified_time" content={modifiedTime} />
    )}
    {ogType === "article" && <meta property="article:author" content="Rian Touchent" />}
    {ogImageUrl && <meta property="og:image" content={ogImageUrl} />}
    {ogImageUrl && <meta property="og:image:width" content="1200" />}
    {ogImageUrl && <meta property="og:image:height" content="630" />}
    {ogImageUrl && ogImageAlt && <meta property="og:image:alt" content={ogImageAlt} />}
    {ogImageUrl && <meta name="twitter:card" content={ogImageCard} />}
    {ogImageUrl && <meta name="twitter:image" content={ogImageUrl} />}
    <meta name="twitter:title" content={title} />
    {description && <meta name="twitter:description" content={description} />}
    <link rel="alternate" type="application/rss+xml" title="Rian Touchent" href="/rss.xml" />
  </head>
  <body class={bodyClass}>
    <slot />
  </body>
</html>
